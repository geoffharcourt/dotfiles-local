# No arguments: `git status`
# With arguments: acts like `git`
g() {
  if [[ $# > 0 ]]; then
    gh $@
  else
    command git status
  fi
}

# Looks like this has to be designated after the function is defined rather than
# in the completions folder.
compdef g=git
